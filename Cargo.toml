[workspace]
members = [
    "reflect",
    "reflect-cli",
    "reflect-codegen",
    "reflect-demo",
    "reflect-derive",
    "reflect-schema",
]
resolver = "2"

[workspace.dependencies]
reflect = { path = "reflect" }
reflect-derive = { path = "reflect-derive" }
reflect-schema = { path = "reflect-schema" }
reflect-codegen = { path = "reflect-codegen" }
